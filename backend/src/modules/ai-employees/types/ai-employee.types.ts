// backend/src/modules/ai-employees/types/ai-employee.types.ts
// Intentionally empty — no additional types beyond AIEmployeeConfiguration needed at this phase.
// Import directly from './ai-employee-configuration.types' when needed,
// or via the barrel index.ts which re-exports it.
export {};
